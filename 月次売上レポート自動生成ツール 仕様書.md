# 月次売上レポート自動生成ツール 仕様書

2026-09-22 · @Someone

## 1. 概要

### 目的

PostgreSQLに蓄積した売上データから、月次売上レポートのたたき台（PowerPoint）を自動生成する。担当者は生成された資料の所見やレイアウトを手直しするだけでよい状態にし、毎月の資料作成の工数を減らす。

### スコープ

- 対象：月次売上レポート（1テーマ）。将来ほかのテーマに流用できる構造にする
- 含む：サンプルDB（24か月分）、集計ビュー、PPTX生成ロジック、Streamlitの操作画面、pgAdminでのDB管理
- 含まない：本番の認証基盤、既存の基幹システムとの連携、定期実行（将来の拡張として扱う）

### 想定利用者

| 利用者 | 使い方 |
| --- | --- |
| 営業企画・経営企画の担当者 | Streamlitで対象月を選び、レポートを生成・ダウンロードする |
| 開発者・管理者 | pgAdminでデータを確認・メンテナンスする。生成ロジックを改修する |

### 技術スタック

| 区分 | 採用技術 |
| --- | --- |
| 言語 | Python 3.12 |
| DB | PostgreSQL 16 |
| DB管理 | pgAdmin 4 |
| 画面 | Streamlit |
| DB接続・集計 | SQLAlchemy 2.x ＋ psycopg 3、pandas |
| PPTX生成 | python-pptx（ネイティブグラフを使用） |
| 実行環境 | Docker Compose |

## 2. システム構成

Docker Composeで3つのコンテナを起動する。コンテナ同士は内部ネットワークで通信し、PostgreSQLはホストに公開しない。

```
[ブラウザ]
   │ http://localhost:8501          │ http://localhost:5050
   ▼                                ▼
┌─ app (Streamlit) ──┐      ┌─ pgadmin ────────┐
│ 画面・集計・PPTX生成 │      │ DB管理画面          │
│ ロール: report_ro   │      │ ロール: 管理者        │
└─────────┬──────────┘      └─────────┬──────────┘
          │   内部ネットワーク backend  │
          ▼                           ▼
       ┌─ db (PostgreSQL 16) ─────────────────┐
       │ salesdb / ボリューム pgdataで永続化          │
       │ ホストへのポート公開なし                     │
       └─────────────────────────────────────┘
```

### サービス一覧

| サービス | イメージ | ポート（ホスト） | 役割 |
| --- | --- | --- | --- |
| db | postgres:16 | 公開しない | データ保存。初回起動時に `db/init/` のSQLを実行する |
| app | 自前ビルド（python:3.12-slim） | 127.0.0.1:8501 | Streamlit画面、集計、PPTX生成 |
| pgadmin | dpage/pgadmin4 | 127.0.0.1:5050 | DBの参照・管理 |

### 構成上のルール

- ポートは `127.0.0.1` にバインドし、同じネットワーク内の他端末から直接見えないようにする
- appとpgadminは `depends_on`（`condition: service_healthy`）でdbの起動完了を待つ。dbには `pg_isready` のヘルスチェックを設定する
- データは名前付きボリューム（`pgdata`、`pgadmin_data`）で永続化する
- サンプルデータを作り直すときは `docker compose down -v` でボリュームごと削除する

## 3. ディレクトリ構成

```
sales-report/
├─ docker-compose.yml
├─ .env.example            # 環境変数の雛形（.env はGit管理外）
├─ .gitignore
├─ db/
│  └─ init/                # 初回起動時に番号順で実行
│     ├─ 01_schema.sql     # テーブル定義
│     ├─ 02_views.sql      # 集計ビュー
│     ├─ 03_seed.sql       # サンプルデータ（generate_seriesで生成）
│     └─ 04_roles.sh       # 読み取り専用ロール作成（環境変数を使うためsh）
├─ pgadmin/
│  └─ servers.json         # 接続先の事前登録（パスワードは含めない）
└─ app/
   ├─ Dockerfile
   ├─ requirements.txt
   ├─ .streamlit/
   │  └─ config.toml       # showErrorDetails=false など
   ├─ app.py               # Streamlit画面（入口）
   ├─ cli.py               # CLI実行用の入口（テスト・将来の定期実行用）
   ├─ report/
   │  ├─ db.py             # DB接続。接続情報はここだけで扱う
   │  ├─ queries.py        # データ取得（DataFrameを返す）
   │  ├─ metrics.py        # 集計・前年比・達成率 → ReportData
   │  ├─ insights.py       # 所見コメント生成（ルールベース）
   │  ├─ builder.py        # 共通の描画部品（KPIカード・表・グラフ）
   │  ├─ slides/           # スライドビルダー（1スライド1ファイル＋SLIDESレジストリ）
   │  └─ generate.py       # generate_slide() / generate_report() -> bytes
   └─ templates/
      └─ base.pptx         # スライドマスター（社内書式に差し替え可）
```

### レイヤーの責務

| レイヤー | 責務 | 依存してよいもの |
| --- | --- | --- |
| db / queries | SQLを実行しDataFrameを返す | SQLAlchemy、pandas |
| metrics / insights | 数値の集計と所見文の生成 | pandasのみ（DBもpptxも知らない） |
| builder | ReportDataをスライドに描画する | python-pptxのみ（DBを知らない） |
| generate | 上記をつなぐ | 全レイヤー |
| app / cli | 入力を受け取り generate を呼ぶ | generate、Streamlit |

テーマを追加するときは queries / metrics / builder のテーマ別モジュールを足し、グラフやKPIカードなどの描画部品は共通化して使い回す。

## 4. DB設計

売上明細を中心にマスタを配置するスター型の構成とする。スキーマ名は `sales`。金額は円単位の整数（`integer`、合計値は `bigint`）で持つ。

### 4.1 products（商品マスタ）

| カラム | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| product\_id | serial | PK | 商品ID |
| product\_name | varchar(100) | NOT NULL | 商品名 |
| category | varchar(50) | NOT NULL | 大分類 |
| sub\_category | varchar(50) |  | 小分類 |
| list\_price | integer | NOT NULL | 定価 |
| cost\_price | integer | NOT NULL | 原価（粗利計算用） |

### 4.2 customers（顧客マスタ）

| カラム | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| customer\_id | serial | PK | 顧客ID |
| customer\_name | varchar(100) | NOT NULL | 顧客名（サンプルは架空の名称） |
| customer\_type | varchar(10) | CHECK（法人/個人） | 顧客区分 |
| region | varchar(20) | NOT NULL | 地域 |
| first\_purchase\_date | date |  | 初回購入日（新規・既存の判定用） |

### 4.3 stores（店舗・拠点マスタ）

| カラム | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| store\_id | serial | PK | 店舗ID |
| store\_name | varchar(100) | NOT NULL | 店舗名 |
| region | varchar(20) | NOT NULL | 地域（北海道・東北・関東・中部・関西・九州など） |
| channel | varchar(10) | CHECK（店舗/EC） | 販売チャネル |

### 4.4 sales（売上明細）

| カラム | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| sale\_id | bigserial | PK | 明細ID |
| sale\_date | date | NOT NULL | 売上日 |
| product\_id | integer | FK → products | 商品 |
| customer\_id | integer | FK → customers | 顧客 |
| store\_id | integer | FK → stores | 店舗 |
| quantity | integer | NOT NULL, > 0 | 数量 |
| unit\_price | integer | NOT NULL | 販売単価 |
| discount | integer | NOT NULL DEFAULT 0 | 値引額 |
| amount | integer | NOT NULL | 売上金額（quantity × unit\_price − discount） |

インデックス：`sale_date`、`(store_id, sale_date)`、`(product_id, sale_date)`

### 4.5 targets（月次予算）

| カラム | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| year\_month | date | PK（複合） | 対象月（月初日で保持） |
| store\_id | integer | PK（複合）, FK → stores | 店舗 |
| target\_amount | bigint | NOT NULL | 売上予算 |

### 4.6 サンプルデータ

| 項目 | 内容 |
| --- | --- |
| 期間 | 2024-09〜2026-08（24か月） |
| 件数の目安 | 商品 50、顧客 500、店舗 10（うちEC 1）、明細 約10万件 |
| 仕込む傾向 | 12月と3月の繁忙期、2月と8月の閑散期。ECは前年比で増加、一部のカテゴリは減少。予算未達の店舗を2〜3店舗作る |
| 生成方法 | `03_seed.sql` で `generate_series` と `setseed()` を使い、乱数を固定して毎回同じデータになるようにする |

### 4.7 ロール

| ロール | 権限 | 使用者 |
| --- | --- | --- |
| POSTGRES\_USER（管理者） | 全権限 | 初期化スクリプト、pgAdmin |
| report\_ro | `sales` スキーマの SELECT のみ | Streamlitアプリ |

## 5. 集計ビュー

月単位の集計はDB側のビューに寄せ、Python側は「対象月で絞り込む」「前年比や達成率を計算する」だけにする。月は `date_trunc('month', sale_date)::date` で表す。

| ビュー | 粒度 | 主な列 | 使うスライド |
| --- | --- | --- | --- |
| v\_monthly\_sales | 月 | month, amount, gross\_profit, quantity, order\_count | サマリー、売上推移 |
| v\_monthly\_category | 月 × カテゴリ | month, category, amount, gross\_profit | カテゴリ別売上 |
| v\_monthly\_store | 月 × 店舗 | month, store\_id, store\_name, region, channel, amount, target\_amount | 地域・店舗別実績 |
| v\_monthly\_product | 月 × 商品 | month, product\_id, product\_name, category, amount, quantity | 商品ランキング |
| v\_monthly\_customer | 月 × 顧客 | month, customer\_id, customer\_type, is\_new, amount | 顧客分析 |

### 指標の定義

| 指標 | 定義 |
| --- | --- |
| 売上 | `SUM(amount)` |
| 粗利 | `SUM(amount − quantity × cost_price)` |
| 粗利率 | 粗利 ÷ 売上 |
| 前月比 | 当月 ÷ 前月 − 1 |
| 前年同月比 | 当月 ÷ 前年同月 − 1（前年データがなければ「―」と表示） |
| 予算達成率 | 売上 ÷ 予算 |
| 新規顧客 | first\_purchase\_date が対象月内にある顧客 |
| 客単価 | 売上 ÷ 購入顧客数 |

除算の分母が0の場合は値を持たず「―」と表示する。

## 6. レポート（PPTX）仕様

### 6.1 共通仕様

- サイズは16:9。`templates/base.pptx` のスライドマスターとレイアウトを使う
- グラフはpython-pptxのネイティブグラフで作り、PowerPoint上でデータを編集できるようにする
- 金額は「百万円」単位で小数点1桁、比率は小数点1桁の％で表示する
- 強調表示：前年比マイナスや予算未達は赤、達成は緑。色はテンプレート側の定数で定義する
- 各スライドのノート欄に、集計条件（対象月、フィルタ、生成日時）を記載する
- ファイル名：`月次売上レポート_YYYY-MM.pptx`（拠点を絞った場合は末尾に拠点名）

### 6.2 スライド構成

| No | スライド | 内容 | 表現 |
| --- | --- | --- | --- |
| 1 | 表紙 | タイトル、対象月、作成日 | テキスト |
| 2 | サマリー | 売上、粗利、前月比、前年同月比、予算達成率、要点3行 | KPIカード＋箇条書き |
| 3 | 売上推移 | 直近12か月の売上と前年同期 | 折れ線グラフ（2系列） |
| 4 | カテゴリ別売上 | カテゴリ別の売上・構成比・前年比 | 横棒グラフ＋表 |
| 5 | 地域・店舗別実績 | 店舗別の売上・予算・達成率（達成率の高い順） | 表（未達は赤） |
| 6 | 商品ランキング | 売上トップ10と前年比ワースト5 | 表（2つ並べる） |
| 7 | 顧客分析 | 新規・既存の売上比率、顧客区分別売上、客単価 | 円グラフ＋KPI |
| 8 | 所見・課題 | 自動生成した所見と、担当者が記入する欄 | 箇条書き |

### 6.3 所見の自動生成（ルールベース）

初版はルールベースで生成する。各ルールは条件と文のテンプレートを持ち、該当したものを重要度の高い順に最大5件まで出す。

| ルール | 条件 | 文の例 |
| --- | --- | --- |
| 予算達成 | 全体の達成率が100％以上／未満 | 「全体の予算達成率は96.3％で、予算を下回った。」 |
| 前年比の大きな変動 | 前年比が±10％を超えるカテゴリ | 「○○カテゴリが前年比+18.2％と伸びた。」 |
| 未達店舗 | 達成率が90％未満の店舗 | 「△△店は達成率84.5％で要確認。」 |
| チャネル | ECの売上構成比が前年から2ポイント以上変動 | 「EC比率が前年の12.1％から15.4％に上昇。」 |
| 粗利率 | 前月から1ポイント以上変動 | 「粗利率が前月比−1.4ポイント。」 |

閾値は `insights.py` の定数としてまとめ、あとから調整できるようにする。将来はLLMに集計結果（数値のみ）を渡して文章を生成する方式に差し替えられるよう、所見の生成はインターフェース（`generate_insights(data) -> list[str]`）で切り出す。

### 6.4 出力パターン

レポートは次の2つのパターンで出力できるようにする。どちらも同じスライド部品を使い、違うのは「何枚をまとめて出すか」だけとする。

| パターン | 内容 | 出力ファイル | 主な用途 |
| --- | --- | --- | --- |
| A. 1ページずつ出力 | スライドを1種類選び、1枚だけのファイルを生成する | `月次売上レポート_YYYY-MM_<スライドID>.pptx` | スライドごとの開発・確認。既存資料に1枚だけ差し込む |
| B. 一括出力 | 選択した複数のスライド（初期値は全スライド）を1ファイルにまとめる | `月次売上レポート_YYYY-MM.pptx` | 毎月の資料のたたき台作成 |

### 6.5 スライドの部品化

2パターンを両立させるため、各スライドを独立した部品（スライドビルダー）として実装し、レジストリに登録する。

```python
# report/slides/__init__.py
SLIDES: dict[str, SlideBuilder] = {
    "cover": build_cover,          # 1 表紙
    "summary": build_summary,      # 2 サマリー
    "trend": build_trend,          # 3 売上推移
    "category": build_category,    # 4 カテゴリ別売上
    "store": build_store,          # 5 地域・店舗別実績
    "product": build_product,      # 6 商品ランキング
    "customer": build_customer,    # 7 顧客分析
    "insights": build_insights,    # 8 所見・課題
}

# 各ビルダーの共通シグネチャ：受け取ったPresentationに1枚追加する
# def build_xxx(prs: Presentation, data: ReportData) -> None
```

- 各ビルダーは自分のスライドに必要なデータだけを `ReportData` から読む。他のスライドの出力結果に依存しない
- パターンAでは、対象スライドに必要なクエリだけを実行する
- 所見スライドは全データを使うため、単独で出力した場合も必要な集計をすべて実行する

### 6.6 生成関数

```python
def generate_slide(month: date, slide_id: str, region: str | None = None) -> bytes:
    """パターンA：指定した1枚だけのpptxを返す"""

def generate_report(
    month: date,
    region: str | None = None,
    slides: list[str] | None = None,   # None = 全スライド
    with_insights: bool = True,
) -> bytes:
    """パターンB：選択したスライドを順番にまとめたpptxを返す"""
```

両関数とも同じスライドビルダーを呼ぶため、1枚ずつ出しても一括で出しても、同じスライドの中身は一致する。

### 6.7 開発の進め方

いきなり8枚を作らず、段階的に作る。

| 段階 | 内容 | 確認方法 |
| --- | --- | --- |
| Step 1 | 環境構築（Docker Compose、DB、サンプルデータ、集計ビュー） | pgAdminでデータとビューを確認 |
| Step 2 | パターンAの仕組み＋「サマリー」1枚 | 1枚出力して数値を確認 |
| Step 3 | 「売上推移」「カテゴリ別」など、スライドを1枚ずつ追加 | パターンAで各スライドを確認 |
| Step 4 | パターンB（一括出力）と表紙・所見スライド | 8枚組を出力して通しで確認 |

## 7. Streamlit画面仕様

出力パターンごとに `st.tabs` で画面を分ける。対象月と拠点は両タブ共通としてサイドバーに置く。

```
┌─ サイドバー ────────┐ ┌─ メイン ───────────────────────────────┐
│ 対象月 [2026-08 ▼]  │ │ [ 1ページずつ出力 | 一括出力 ]  ←タブ  │
│ 拠点   [全体    ▼]  │ │ （選択中のタブの内容）                 │
└─────────────────────┘ └───────────────────────────────────────────┘
```

### 7.1 サイドバー（共通）

| 項目 | 部品 | 選択肢・初期値 |
| --- | --- | --- |
| 対象月 | st.selectbox | データが存在する月。初期値は最新の月 |
| 拠点 | st.selectbox | 全体／地域ごと。初期値は全体 |

### 7.2 タブA：1ページずつ出力

| 項目 | 部品 | 内容 |
| --- | --- | --- |
| スライド | st.radio | 6.2の8種類から1つ選ぶ。初期値はサマリー |
| プレビュー | st.metric / st.line\_chart / st.dataframe | 選んだスライドに載る数値やグラフを画面上で表示する |
| 生成 | st.button → st.download\_button | `generate_slide()` で生成し、1枚のpptxをダウンロードさせる |

### 7.3 タブB：一括出力

| 項目 | 部品 | 内容 |
| --- | --- | --- |
| 含めるスライド | st.multiselect | スライド3〜8。初期値はすべて選択（表紙とサマリーは常に出力） |
| 所見を自動生成 | st.checkbox | 初期値ON |
| プレビュー | st.metric / st.line\_chart | サマリーのKPIと売上推移 |
| 生成 | st.button → st.download\_button | `generate_report()` で生成し、まとめたpptxをダウンロードさせる |

### 7.4 共通の動作

1. DBの取得結果は `@st.cache_data(ttl=600)` でキャッシュし、タブ間で共有する
2. 生成中は `st.spinner` で処理中を表示する
3. 生成したpptxのバイト列はタブごとに別のキーで `st.session_state` に保持する。サーバーのディスクには保存しない
4. 対象月や拠点を変更したら、保持中の生成結果を破棄する（古い条件のファイルを誤ってダウンロードさせない）
5. エラー時は「レポートの生成に失敗しました」とだけ表示し、詳細はサーバーのログに出す

### 7.5 CLI

画面と同じ関数を呼ぶ。

```bash
python cli.py --month 2026-08 --slide summary       # パターンA
python cli.py --month 2026-08                       # パターンB（全スライド）
python cli.py --month 2026-08 --slides trend,store  # パターンB（選択）
```

## 8. セキュリティ・環境変数

### 8.1 環境変数（.env）

| 変数 | 使うサービス | 説明 |
| --- | --- | --- |
| POSTGRES\_DB | db, app | DB名（salesdb） |
| POSTGRES\_USER | db | 管理者ユーザー |
| POSTGRES\_PASSWORD | db | 管理者パスワード |
| REPORT\_DB\_USER | db, app | 読み取り専用ユーザー（report\_ro） |
| REPORT\_DB\_PASSWORD | db, app | 読み取り専用ユーザーのパスワード |
| DB\_HOST / DB\_PORT | app | db / 5432 |
| PGADMIN\_DEFAULT\_EMAIL | pgadmin | pgAdminのログインID |
| PGADMIN\_DEFAULT\_PASSWORD | pgadmin | pgAdminのログインパスワード |

- `.env` はGit管理外とし、リポジトリには値を空にした `.env.example` だけを置く
- 各サービスには、必要な変数だけを `environment:` で個別に渡す。appに管理者パスワードやpgAdminの認証情報は渡さない

### 8.2 サーバーとブラウザの境界

Streamlitのコードはすべてサーバー（appコンテナ）で動き、ブラウザに届くのは `st.*()` に渡した値だけである。この前提で次を守る。

- 接続情報は `report/db.py` の中だけで読み込む。画面側にはDataFrameや数値だけを返す
- `os.environ`、設定オブジェクト、エンジンや接続オブジェクトを `st.write` などで表示しない
- 画面に出すDataFrameは表示用に列を絞る。顧客名などは集計後の値だけを扱う
- PPTXのノート欄には集計条件だけを書き、SQLや接続先は書かない

### 8.3 Streamlitの設定（.streamlit/config.toml）

```toml
[client]
showErrorDetails = false   # トレースバックを画面に出さない
toolbarMode = "viewer"     # 開発者メニューを隠す

[server]
headless = true

[browser]
gatherUsageStats = false
```

### 8.4 DB・pgAdmin

- appは読み取り専用ロール `report_ro` で接続する。SQLはプレースホルダで値を渡し、文字列連結で組み立てない
- PostgreSQLのポートはホストに公開しない。外部からの参照はpgAdmin経由とする
- pgAdminは管理者権限で操作できるため、ポートは `127.0.0.1` に限定する。`servers.json` にはパスワードを含めない
- 社内に公開する場合は、前段に認証付きのリバースプロキシを置くか、VPN内だけで公開する。pgAdminは公開しない

## 9. 起動手順と今後の拡張

### 9.1 起動手順

```bash
cp .env.example .env        # パスワード類を設定する
docker compose up -d --build
```

| 画面 | URL |
| --- | --- |
| Streamlit（レポート生成） | http://localhost:8501 |
| pgAdmin | http://localhost:5050 |

- 初回起動時に `db/init/` のSQLが実行され、テーブル・ビュー・サンプルデータ・ロールが作成される
- データを作り直すときは `docker compose down -v` のあとに再起動する
- CLIで生成する場合：`docker compose exec app python cli.py --month 2026-08`

### 9.2 受け入れ条件

- `docker compose up` のみで3サービスが起動し、StreamlitからPPTXをダウンロードできる
- 生成したPPTXがPowerPointで開け、グラフのデータを編集できる
- サマリーの売上合計が、pgAdminで実行した `SELECT SUM(amount)` の結果と一致する
- エラーを発生させても、画面に接続情報やトレースバックが表示されない
- appコンテナから更新系SQLを実行すると権限エラーになる

### 9.3 今後の拡張

| 拡張 | 内容 |
| --- | --- |
| LLMによる所見生成 | `generate_insights()` をClaude APIなどを使う実装に差し替える。渡すのは集計後の数値のみ |
| 定期実行 | cronなどで毎月1日に `cli.py` を実行し、共有フォルダやチャットに届ける |
| テーマ追加 | 営業パイプライン、在庫状況など。queries / metrics / builder をテーマ別に追加する |
| 社内テンプレート対応 | `base.pptx` を社内書式に差し替え、レイアウト名で対応付ける |
| 認証 | 社内公開時にリバースプロキシ＋SSOを前段に置く |
